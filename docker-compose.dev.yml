services:
    backend:
        env_file:
            - ./backend/.env.development
        environment:
            NODE_ENV: development
    frontend:
        env_file:
            - ./frontend/.env.development
        environment:
            NODE_ENV: development
