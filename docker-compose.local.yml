services:
    backend:
        env_file:
            - ./backend/.env.local
        environment:
            NODE_ENV: development
    frontend:
        env_file:
            - ./frontend/.env.local
        environment:
            NODE_ENV: development
